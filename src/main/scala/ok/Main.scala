package ok

import kocko.clasment._

object Main extends App {

  // test de la classe
  val it = new IntIterator(10)
  println(it.next())
  println(it.next)

  // creation of new object
  val newFings = new kokoClass( coco = Person, sizing = 4)

  newFings.get_obj("test")
}
